# The URL of your deployed Resume Parser API (the FastAPI service on Hugging Face Spaces)
# This is a PUBLIC variable (NEXT_PUBLIC_ prefix) so it's bundled into the frontend.
# That's fine — it's just an API endpoint, not a secret.

NEXT_PUBLIC_API_URL=https://tahir283417-resume-parser.hf.space
